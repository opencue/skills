interface:
  display_name: "MyVPS"
  short_description: "Create remote Supabase schemas and apply local SQL files over SSH."
  default_prompt: "Create a schema or apply local SQL to my remote Supabase server (target taken from SUPA_SCHEMA_SSH_TARGET) using supabase-create-schema and supabase-apply-sql."
