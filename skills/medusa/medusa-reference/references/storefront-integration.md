# Storefront Integration

- Keep storefront request/response contracts aligned with backend DTOs.
- Normalize data at the boundary and keep component props stable.
- Preserve locale, route context, and selected entity context when editing flows.
- Prefer incremental UI edits over broad rewrites unless requested.
